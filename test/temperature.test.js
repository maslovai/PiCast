'use strict';

const expect = require('expect');
const temp = require('../lib/light-display/temperature.js');
const Gpio = require('onoff').Gpio;

describe('Temperature module', function(){

  it('should light up all lights when 90 or above', function() {

    let temp = '90';

    expect();

  });

});
